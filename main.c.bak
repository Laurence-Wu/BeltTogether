#include <avr/io.h>
#include <util/delay.h>

int main(void) {
    // Set pin 13 (PB5 on Arduino Uno) as output for LED
    DDRB |= (1 << PB5);

    while(1) {
        // Toggle LED
        PORTB ^= (1 << PB5);
        _delay_ms(1000);
    }

    return 0;
}
